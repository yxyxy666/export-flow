# 前端规则

## 边界

页面/组件只负责展示和用户交互；业务规则放在可测试的 domain/service 层；网络访问集中在 API client；不得在组件内散落请求、鉴权和数据转换。

## 状态与错误

明确区分 loading、empty、error、success；异步操作可取消或防止过期响应覆盖新状态。用户可见错误不得泄露内部堆栈、令牌或隐私字段。

## 可访问性与兼容

交互控件具备键盘操作、可见焦点、语义标签和可读错误。响应式布局不得依赖固定视口宽度；新增浏览器支持范围写入 ADR 或待决策文档。

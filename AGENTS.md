# 项目协作基线

本文件是所有模型和贡献者进入本项目后的第一阅读入口，也是项目文档基线和索引。它定义各类文档的位置与职责，不承载各领域的全部细则。

## 必读顺序

1. 本文件：项目地图、文档职责和查阅路径。
2. `.specify/memory/constitution.md`：SDD/TDD 工作流、风险分级和门禁。
3. 与当前任务对应的 `docs/rules/*.md`：局部工程规则。
4. 当前变更目录下的 `specs/<feature-dir>/`：标准规格产物（中高风险任务必需）。
5. `docs/adr/`：已生效的架构决策。

## 基本约束

- 先按 `.specify/memory/constitution.md` 的“风险分级”章节判断风险，再决定是否启动规格流程；中高风险按“需求规格 → 澄清（可选）→ 技术计划 → 任务拆分”阶段执行，不得跳过规格和项目测试门禁。
- 规格流程只负责开发阶段的需求、计划和任务拆分；构建、打包、测试执行、容器和发布遵循 `docs/rules/` 中的项目工具链规则。
- 先写测试、确认测试失败或覆盖缺口，再写实现；不得反向补测试掩盖行为。
- 每条验收标准必须使用稳定的 `AC-xxx` 编号，并在 `tasks.md` 中映射到一个或一组测试任务；测试用例必须在名称、注释或元数据中引用对应 `AC-xxx`。实现完成前必须确认不存在未映射或仅有任务、没有测试的验收标准。
- 文档、测试、实现和 ADR 必须互相引用。
- 不确定的事项写入 `docs/decisions-pending.md`，不得自行伪造已确认的技术选型。
- 项目维护文档和功能规格一律使用中文；标准文件名、代码符号和协议字段名可以保留英文。

## 规则权级与开发治理

本文件是项目最高管理者。`.specify/memory/constitution.md` 是开发阶段管理者，负责
SDD/TDD、风险、规格评审和实现门禁，但不得与本文件冲突。`docs/rules/` 提供各工程领域的执行细则，`specs/<feature-dir>/` 只描述单次功能。

规则优先级为：

```text
AGENTS.md > .specify/memory/constitution.md > docs/rules/ > specs/<feature-dir>/ > 实现代码
```

发生冲突时必须暂停当前工作，以本文件为准修正文档后再继续。修改开发原则时先更新
本文件，再同步 constitution；不得只修改 constitution。

## 项目地图与文档职责

| 路径 | 用途 | 维护时机 |
| --- | --- | --- |
| `AGENTS.md` | 模型入口、项目地图和不可绕过的总原则 | 总规则或目录职责变化时 |
| `docs/rules/` | 前后端、测试、脚本、构建、容器、命名等局部规则 | 对应规则变化时 |
| `.specify/memory/constitution.md` | 项目开发原则和开发阶段门禁 | 原则变化时同步更新 |
| `specs/<feature-dir>/` | 标准规格产物：`spec.md`、`plan.md`、`tasks.md` 及其附属产物 | 中高风险变更开始前，实施中同步 |
| `docs/adr/` | 不可逆或跨模块架构决策 | 决策落地前/后 |
| `tests/` | 自动化测试和测试夹具 | 需求或行为变化时 |
| `src/` | 生产代码（待项目初始化后按技术栈细化） | 实现阶段 |

## 按任务查阅

- 新功能或行为变化：先读本文件和 `.specify/memory/constitution.md`，再读对应领域规则、规格、测试和实现。
- 架构、依赖或数据存储变化：先读 `docs/adr/`，必要时新增 ADR。
- CI、打包、发布、脚本：读 `docs/rules/build.md` 和 `docs/rules/scripts.md`。
- 容器或本地启动：读 `docs/rules/container.md`。
- 前端/后端实现：分别读 `frontend.md`、`backend.md`、`naming.md`。
- 测试设计与回归修复：读 `docs/rules/testing.md`。

## 变更目录命名

项目规格流程管理 `specs/<feature-dir>/` 的命名和文件结构。核心文件为：

- `spec.md`：用户故事、范围、验收标准、非目标和需求约束。
- `plan.md`：技术方案、研究、数据模型、契约目录和实现结构。
- `tasks.md`：按用户故事组织的可执行任务。

计划阶段按功能需要生成以下附属产物：

| 产物 | 作用 | 生成条件 |
| --- | --- | --- |
| `research.md` | 记录技术调查、候选方案、验证结果和最终选择理由 | 存在技术不确定性、外部依赖或需要方案比较时 |
| `data-model.md` | 记录实体、字段、类型、关系、约束、状态和数据生命周期 | 功能涉及数据结构、数据库、持久化或复杂状态时 |
| `contracts/` | 记录 API、事件、消息、插件或模块之间的输入输出契约 | 功能跨模块、跨服务或有外部集成边界时 |
| `quickstart.md` | 记录开发者如何启动、配置、操作和验证本功能 | 功能需要本地演示、手工验证或新增运行步骤时 |

这些附属产物不是每个功能都必须存在；一旦生成，就必须和 `spec.md`、`plan.md`、
`tasks.md` 及测试保持一致。不能因为文件未生成就省略规格中已经确认的内容。

## 待确认决策

见 [`docs/decisions-pending.md`](docs/decisions-pending.md)。确认后必须转为 ADR、更新 constitution 或更新对应规则。
